{% extends "hepiaThanatosBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Ensemble{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Ensemble</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Liste des ensembles
                    </div>
                    <div class="panel-body">
                        <table width="100%" class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Titre</th>
                                    <th>Description</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for data in datas %}
                                    <tr onclick="window.document.location = '{{ path('hepia_thanatos_set_view', {'id': data.id}) }}';" style="cursor:pointer;">
                                        <td>{{ data.title }}</td>
                                        <td>{{ data.description }}</td>
                                    </tr>
                                {% endfor %} 
                            </tbody>
                        </table>
                        {% include 'hepiaThanatosBundle::pagination.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}