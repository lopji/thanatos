{% extends "hepiaThanatosBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Réseau de neurones{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Réseau de neurones</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Liste des réseaux de neurones
                    </div>
                    <div class="panel-body">
                        <table width="100%" class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Titre</th>
                                    <th>Description</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for network in networks %}
                                    <tr onclick="window.document.location = '{{ path('hepia_thanatos_network_view', {'id': network.id}) }}';" style="cursor:pointer;">
                                        <td>{{ network.title }}</td>
                                        <td>{{ network.description }}</td>
                                    </tr>
                                {% endfor %} 
                            </tbody>
                        </table>
                        {% include 'hepiaThanatosBundle::pagination.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}